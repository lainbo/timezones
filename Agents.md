# 同刻 · 维护指南

本文供后续维护者和编码代理使用。修改前先阅读相关实现，以当前代码、`package.json` 和 `pnpm-lock.yaml` 核实行为与依赖版本。功能或约定发生变化时，同步维护本文与 `README.md`。

## 必须遵守：只描述最终实现

**代码、注释、界面文案和交付文档只表达当前正确的实现。不要把纠错过程、已放弃的方案或曾经犯过的错误附加到最终结果里。**

- 用户纠正实现后，直接把代码改正确，同时清理失效的注释和说明。不要在正确结论后追加对旧错误的否定、辩解或更正记录。
- 例如，曾把 `1 + 1` 的结果写成 `3`，被纠正后应直接表达 `1 + 1 = 2`。**禁止写成 `1 + 1 = 2（不等于 3）`，也不要追加“之前写成了 3”“已修正错误结果”等注释。** 这些内容只是在重复已经解决的错误，对理解当前实现没有帮助。
- 注释只解释当前代码中不明显但必要的原因，例如第三方组件的输入约束、时区语义或动画角度的计算原因。显而易见的代码行为不需要注释。
- 用户调整方向后，按最终方案重新组织说明，不在旧说明上叠加“撤销了什么”“没有采用什么”。仅在本次任务中试做后撤销的内容，不列为最终功能、修复或取舍。
- PR 描述、提交说明和任务回复依据实际交付内容编写，不夹带对话记录。涉及已发布行为、真实历史回归、尚存风险，或用户明确要求复盘时，如实说明相关事实。

提交前检查每条新增注释：它是否帮助维护者理解当前实现？如果只是为了证明某个旧错误已经被改正，删除它。

## 项目概况

- 名称：同刻（Same Moment），中文世界时钟与时区换算工具，纯前端应用。
- 核心用途：修改任意城市的当地日期或时间，其他时区同步换算，显示 UTC 偏移、相对时差和跨日提示。
- 设计参考：<https://www.timezones.digital/>。具体交互遵循本项目现有约定。
- 默认卡片：台北、洛杉矶、伦敦、东京；默认基准为 `Asia/Taipei`，默认使用 24 小时制。
- 用户交流和维护说明使用简体中文。用户未明确指定时区和时间制时，按 `Asia/Taipei` 的 24 小时制理解。
- 开发环境：macOS、zsh、Zed。文件统一使用 LF 换行。

## 技术栈与命令

React 19、TypeScript 6、Vite 8（Rolldown）、Tailwind CSS 4、shadcn/ui（Radix）、Lucide、framer-motion、dnd-kit。代码检查使用 Oxlint，格式化使用 Oxfmt。

使用 Node.js 22.12+ 或 24+、pnpm 12。包管理器版本由 `package.json` 的 `packageManager` 指定。安装或更新依赖前先核对锁文件，使用 pnpm 并同步更新 `pnpm-lock.yaml`。

| 命令                  | 用途                               |
| --------------------- | ---------------------------------- |
| `pnpm install`        | 安装依赖                           |
| `pnpm dev`            | 启动本地开发服务                   |
| `pnpm lint`           | Oxlint 检查，警告按失败处理        |
| `pnpm format`         | 使用 Oxfmt 格式化                  |
| `pnpm format:check`   | 只检查格式                         |
| `pnpm typecheck`      | TypeScript 检查                    |
| `pnpm build`          | TypeScript 检查及生产构建          |
| `pnpm check`          | lint、格式检查、类型检查及生产构建 |
| `pnpm preview`        | 预览已有的 `dist` 构建             |
| `pnpm data:timezones` | 从 CLDR 包重新生成中文时区数据     |

开发地址为 <http://localhost:5173/>，Vite 固定监听 `127.0.0.1:5173`，端口被占用时直接报错。不要擅自终止占用端口的进程。

`@/` 指向 `src/`。格式遵循 `.oxfmtrc.json`，包括单引号、不加分号和 LF；检查规则位于 `.oxlintrc.json`。

## 代码导航

| 文件                                | 职责                                                           |
| ----------------------------------- | -------------------------------------------------------------- |
| `index.html`                        | React 启动前恢复主题，设置根元素和浏览器主题色                 |
| `src/main.tsx`                      | 检测原生 Temporal，再动态加载应用；不支持时显示提示            |
| `src/App.tsx`                       | 共享时间点、实时模式、换算基准、卡片顺序和弹窗状态             |
| `src/lib/temporal.ts`               | 原生 Temporal 导出、当地时间解析和显示格式                     |
| `src/lib/timezones.ts`              | 时区枚举、中文信息、搜索、偏移计算及校验                       |
| `src/lib/preferences.ts`            | 卡片与显示偏好的读取、校验和保存                               |
| `src/components/timezone-card.tsx`  | 单张卡片、拖拽手柄、表针和时间刻度                             |
| `src/components/time-editor.tsx`    | 精确输入当地时间，处理夏令时歧义                               |
| `src/components/zone-picker.tsx`    | 时区多选弹窗、搜索和草稿选择                                   |
| `src/components/date-picker.tsx`    | 中文日期选择器及日历组件的日期适配                             |
| `src/components/theme-switcher.tsx` | 日月按钮、主题保存和原生主题过渡                               |
| `src/components/ui/`                | 项目内的 shadcn/ui 组件源码                                    |
| `src/index.css`                     | Tailwind 入口、主题变量、基础样式和 View Transition 伪元素规则 |
| `src/data/timezone-names.json`      | 生成的中文城市、地区和时区别名数据                             |
| `scripts/generate-timezones.mjs`    | 从 Unicode CLDR 提取中文时区数据                               |

## 时间与时区约定

1. **使用浏览器原生 Temporal，不安装或引入 Temporal polyfill。** 目标使用环境为 Chrome 144+。启动时仍以实际 API 检测为准；检测通过前，不得加载会执行 Temporal 调用的业务模块。
2. 全部卡片共享一个 `Temporal.Instant`。使用 `ZonedDateTime` 获取各地时间、日期与偏移，使用 `PlainDateTime` 解释用户输入。不要为每个城市维护独立时间点。
3. `Date` 可以用于第三方组件边界。日历组件要求 `Date`，现有适配固定用 UTC 传递日历日，业务时间换算仍由 Temporal 完成。保持这一边界，防止设备时区改变所选日期。
4. UTC 偏移必须按正在查看的时间点计算。地区时区的偏移会随夏令时变化，不能把“美西”等地区永久映射到某个固定偏移。
5. 当地时间可能落入夏令时空缺或重叠。`resolveLocalTime` 返回 `gap`、`overlap` 或 `exact`：空缺需要提示用户改选；重叠需要用户选择具体的一次发生时刻及其偏移。
6. 当地一天的时长由相邻两个当地日界计算，不能固定为 24 小时。刻度应支持夏令时日期的 23 或 25 小时，以及半小时、四十五分钟偏移。
7. 日期选择范围为 1900 至 2100 年。日期切换按当地日历日计算。
8. 实时显示精确到分钟。手动修改时间后退出实时模式，修改来源城市成为基准；“回到现在”恢复实时模式。

时区目录通过 `Intl.supportedValuesOf('timeZone')` 获取，并补充 `UTC` 及 UTC−12 至 UTC+14 的整小时固定偏移。`Etc/GMT` 标识符的正负号与显示的 UTC 偏移相反，处理时保持现有转换语义。

中文名称来自 Unicode CLDR，搜索还包含项目维护的常见别名。修改数据生成逻辑后运行 `pnpm data:timezones`，检查生成文件并保留 `public/UNICODE-LICENSE.txt`。浏览器只加载提取后的数据，不加载完整 CLDR 包。可选时区数量随浏览器数据库变化，不要写死数量。

## 界面与交互约定

- 页面以工具栏和时区卡片为主，宽度达到 Tailwind `xl`（1280px）时每行四张卡片，更小宽度逐级减少列数。
- 排序只能由专用拖拽手柄触发，保持键盘排序和 Escape 取消能力，避免与大号时间及滑杆操作冲突。
- 卡片下方滑杆使用 **5 分钟步进**。精确到分钟的输入由点击卡片大号时间打开的弹窗完成。
- 添加时区使用模态框与 shadcn Checkbox。支持中文城市和地区名、英文名、IANA 标识符，以及 `+8`、`-7`、`UTC+08:00`、`+5:30`、`+5.75` 等偏移搜索。
- 打开弹窗前已选的时区置顶并保留卡片顺序，其他条目按当前查看时间的 UTC 偏移升序排列。弹窗打开期间使用固定的时间点和已有分组，勾选动作只修改草稿；“完成”才应用，取消或关闭放弃草稿。
- 时区列表圆角条目之间保留间距，目前为 `gap-1.5`（6px）。
- 日期选择器、Checkbox、Select 和弹窗复用 `src/components/ui/` 中的 shadcn 组件。修改这些组件时检查所有调用位置。
- 布局和组件样式使用 Tailwind。全局 CSS 用于主题变量、基础规则和必要的伪元素样式；动态角度、拖拽位移等值可以使用运行时样式。
- 颜色使用语义主题变量，新增界面同时检查深浅主题。指针操作不应产生突兀的焦点边框；键盘操作保留清晰的 `focus-visible` 提示。

## 动画与主题

- 页面进入、表针旋转和回到当前时间的刻度动画使用 framer-motion，现有入口是 `LazyMotion`、`domAnimation` 和 `framer-motion/m`。
- 表针保留连续角度，通过最短角度路径平滑旋转，特别注意 59 分跨整点。卡片拖拽的位移由 dnd-kit 管理，避免其他动画覆盖同一元素的 transform。
- 点击“回到现在”时，各卡片刻度指示器平滑移动；拖动滑杆时及时跟随用户输入。
- 首次访问跟随系统颜色；用户手动切换后，以本地保存的主题为准。系统变化只影响尚未手动选择主题的页面。
- 主题入口为单个日月按钮：太阳表示当前浅色，月亮表示当前深色。点击直接切换。
- 主题切换使用原生 `document.startViewTransition`，通过 `flushSync` 同步提交主题和图标。两个切换方向均在 `::view-transition-new(root)` 上从按钮圆心向外展开，持续 500ms。
- `index.html` 在 React 加载前恢复主题，组件负责后续切换；二者使用一致的存储键、根元素 `.dark` 类、`data-theme` 和 `theme-color`。
- 尊重系统“减少动态效果”偏好，主题切换在此情况下直接更新。

## 本地持久化

| localStorage 键              | 内容                                                                  |
| ---------------------------- | --------------------------------------------------------------------- |
| `same-moment.preferences.v1` | `zones`：时区及其顺序；`base`：基准城市；`hour12`：是否使用 12 小时制 |
| `same-moment.theme`          | 用户手动选择的 `light` 或 `dark`；没有有效值时跟随系统                |

读取偏好时校验时区并去重，移除基准城市后选取可用基准。存储不可用时，当前页面操作仍应生效。自定义换算时间不持久化，刷新后回到实时时间。修改存储结构时考虑已有浏览器数据。

## 仓库与部署

- 开源仓库：<https://github.com/lainbo/timezones>，默认分支 `main`，项目代码使用 MIT 许可证。保留 CLDR 数据的 Unicode 许可证。
- Cloudflare Pages 项目：`tongke-timezones`，线上地址：<https://tongke-timezones.pages.dev/>。
- 部署通过 Cloudflare Pages 的 Git 集成连接该仓库，推送到 `main` 自动发布生产版本。部署配置在 Pages 控制台维护。
- 构建在仓库根目录执行 `pnpm build`，发布目录为 `dist`。Node.js 由 `.node-version` 固定为 `24.20.0`；生产和预览环境使用 `PNPM_VERSION=12.5.1`，与 `packageManager` 保持一致。
- 发布前检查待提交文件，凭据、本地环境文件、`node_modules`、`dist` 和 `.wrangler` 由 `.gitignore` 排除。提交及推送应在当前任务授权范围内进行。
- 发布后确认 Pages 部署成功且关联正确的 Git 提交，再检查线上页面；本地构建通过不能单独代表远端部署完成。

## 修改与验证

- 修改前阅读相关代码、调用位置和已有验证结果，取得足以支持改动的直接证据。代码无法解释行为或涉及历史回归时，再查阅可用的 Git 历史。
- 只实现当前请求及其必要改动。常规实现细节自主完成，不顺手增加功能、通用框架或兼容层。
- 新增或调整第三方 API 用法前，核对实际依赖版本，优先查询对应版本的 Context7 文档；工具不可用时使用官方文档或本地公开类型声明。
- **未经用户明确要求，不新增单元测试等测试代码。** 按改动选择已有检查和实际浏览器验证，不为文档改动扩大验证范围。
- 代码改动通常运行 `pnpm check`。涉及交互和视觉时，还需在本地浏览器验证受影响行为；构建成功不能单独证明交互正确。
- 涉及时区逻辑时，重点核对跨日、不同偏移和夏令时空缺/重叠；涉及界面时，检查对应控件的键盘操作、深浅主题和窄屏布局。根据实际改动选择范围，不机械重跑所有场景。
- 检查通过后，只有新改动、失败或具体疑点才扩大或重复验证。交付时说明实际结果、验证证据和仍存在的限制。

日常运行和用户使用说明见 [README.md](README.md)。
